# Training dependencies (used in Google Colab notebooks)
# Install with: pip install -r requirements-training.txt
# NOTE: torch is NOT listed here — Colab provides it pre-installed.

transformers>=4.45.0
datasets>=3.0.0
scikit-learn>=1.5.0
accelerate>=0.34.0
huggingface-hub>=0.25.0
pyyaml>=6.0.0
jupyter>=1.1.0
matplotlib>=3.9.0
seaborn>=0.13.0
optuna>=4.0.0

# Uncomment if using Weights & Biases:
# wandb>=0.18.0
